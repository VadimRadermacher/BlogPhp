{% extends 'common.twig' %}

{% block content %}
  {% for user in users %}
    <div class="">
      <p>username: {{ user.username }}</p>
      <p>email: {{ user.email }}</p>
    </div>
  {% endfor %}
{% endblock %}
